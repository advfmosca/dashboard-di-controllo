# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon

# Editor
.vscode/
.idea/
*.swp
*.swo

# Node (se in futuro)
node_modules/
package-lock.json
yarn.lock

# Build / temp
dist/
build/
*.log

# Env / segreti — non commitarli MAI
.env
.env.local
*.secret
credentials.json

# Local backup folder (30gg retention) — non pubblicare
_backups/

# Drift monitor log — append-only, stato locale dello scheduled task semaforo-v2-drift-monitor
_automation/drift_log.json

# Python caches
__pycache__/
*.pyc
